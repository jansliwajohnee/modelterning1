plugins {
    id 'java'
    id 'org.springframework.boot' version '4.0.1'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'ioioi.it'
version = '1.0.0'
description = 'ML Model Training Service'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(25)
    }
    sourceCompatibility = JavaVersion.VERSION_25
    targetCompatibility = JavaVersion.VERSION_25
}

repositories {
    mavenCentral()
}

dependencies {
    // Spring Boot Web
    implementation 'org.springframework.boot:spring-boot-starter-web'

    // JDBC - fastest option for bulk operations
    implementation 'org.springframework.boot:spring-boot-starter-jdbc'

    // PostgreSQL Driver
    implementation 'org.postgresql:postgresql'

    // HikariCP (already included in spring-boot-starter-jdbc)
    // Optimized connection pooling

    // Optional: Spring Data JDBC (lighter than JPA, good for simple mappings)
    implementation 'org.springframework.boot:spring-boot-starter-data-jdbc'

    // Actuator for monitoring
    implementation 'org.springframework.boot:spring-boot-starter-actuator'

    // Lombok for cleaner code
    compileOnly 'org.projectlombok:lombok'
    annotationProcessor 'org.projectlombok:lombok'

    implementation("io.github.wuhewuhe:bybit-java-api:1.2.7")

    // Ta4j - Technical Analysis for Java
    // Version 0.22.0 - latest version, requires Java 21+
    implementation 'org.ta4j:ta4j-core:0.22.0'

    // Testing
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

tasks.named('test') {
    useJUnitPlatform()
}
